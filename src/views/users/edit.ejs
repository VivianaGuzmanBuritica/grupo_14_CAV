<!DOCTYPE html>
<html lang="en">
<head>
    <title>REGISTRATE</title>
    <link rel="stylesheet" href="/css/userEdit.css"></link>
</head>
<body> 
    <%- include("../partials/header") %> 
    <main>

        
        <form class="editar" action="/usuario/update/<%= user.id %>?_method=PUT" method="post" enctype="multipart/form-data">
          
            
            <figure><img src="/uploads/avatars/<%= user.fotoPerfil%>" alt=""></figure>
            <fieldset>
                <legend><b>User ID</b></legend>   
                <label for="name"></label>
                <%= user.id %>
                </fieldset>


            <fieldset>
            <legend><b>Nombre y apellido</b></legend>   
            <label for="name"></label>
            <input type="text" name="name" id="name" value="<%= user.name %>">
            </fieldset>
            
            <fieldset>
            <legend><b>Nombre de usuario</b></legend>
            <label for="username"></label>
            <input type="text" name="email" id="email" value="<%= user.email %>" >
            </fieldset>

            
            <fieldset>
                <legend><b>Domicilio</b></legend>
                <label for="domicilio"></label>
                <input type="text" name="domicilio" id="domicilio" value="<%= user.domicilio %>">
            </fieldset>


            <fieldset >
                <legend><b>Tipo de Comercio</b></legend>
            
                <select id="tipo" placeholder="tipo" name="id_tipo">
                    <% for (let i=0; i< tipo.length; i++){ %>

                        <option value='<%= tipo[i].id_tipo %>'>
                            <%=tipo[i].tipo_nombre %>
                        </option>
                        <% } %>

                </select>
            </fieldset>

            <fieldset >
                <legend><b>Rubros de interes</b></legend>
            
                <select id="rubro" placeholder="rubro" name="id_rubro">
                    <% for (let i=0; i< rubro.length; i++){ %>

                        <option value='<%= rubro[i].id_rubro %>'>
                            <%=rubro[i].rubro_nombre %>
                        </option>
                        <% } %>

                </select>
            </fieldset>
            
           
                
                <fieldset class="fotoperfil">
                    <legend><b>Foto de perfil</b></legend>
                    <label for="foto">
                        <input type="file" name="fotoPerfil" id="fotoPerfil">
                    </label>
                </fieldset>

                <fieldset>
                <legend><b>Fecha de nacimiento</b></legend>
                <label for="date"></label>
                <input type="date" name="fechaNacimiento" id="fechaNacimiento" value="<%= user.fechaNacimiento %>">>
                </fieldset>

                           
             
                <input type="submit" value="Guardar">  

                <a id="editPassword" href="/usuario/editPassword/<%= user.id %>">Modificar Contraseña</a>

                <a id="eliminar" href="/usuario/delete/<%= user.id %>">Eliminar Usuario</a>
              
            
        </form>
    </main>
    <%- include("../partials/footer") %>
</body>
</html>
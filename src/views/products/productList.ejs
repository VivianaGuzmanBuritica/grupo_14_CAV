<!DOCTYPE html>
<html lang="es">

<head>
    <%- include("../partials/head") %>
        <title>CAV</title>
        <link rel="stylesheet" href="/css/index.css">
</head>

<body>
    <%- include("../partials/header") %>
        <main>
            <section>
                <% for (let i = 0; i< product.length; i++) {//product.forEach(product=> { %>
                    <article class="article">
                        <% if (product.image) { %>
                            <figure><img src="/uploads/products/<%= product.image%>" alt=""></figure>
                            <% } else { %>
                                <figure><img src="/images/default.jpg" alt=""></figure>
                                <% } %>
                                    <section>
                                        <p>
                                            <%= product.name %>
                                        </p>
                                        <p>$<%= product.price %>
                                        </p>
                                        <div class="botones-home">
                                            <span class="botones"><a href="/productDetail/<%= product.id_product %>"><i
                                                        class="far fa-eye"></i>DETALLES</span></a>
                                            <a href="productCart"><span class="botones"><i
                                                        class="fas fa-shopping-cart"></i>COMPRAR</span></a>
                                        </div>

                                    </section>
                    </article>
                    <% } %>
            </section>
        </main>
        <%- include("../partials/footer") %>
</body>

</html>